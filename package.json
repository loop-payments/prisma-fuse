{
  "name": "prisma-fuse",
  "version": "0.0.1",
  "description": "Fuse multiple Prisma schema files into one.",
  "author": "engineering@loop.com",
  "license": "MIT",
  "main": "./dist/cli.js",
  "bin": {
    "prisma-merge": "./dist/cli.js"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "yarn tsc",
    "test": "jest",
    "test:cli": "./dist/cli.js -b 'fixture/prisma/base.prisma' -o 'fixture/prisma/schema.prisma' -s 'fixture/**/*.prisma' -e 'fixture/**/schema.prisma' --verbose"
  },
  "devDependencies": {
    "@jest/globals": "^29.3.1",
    "@types/glob": "^8.0.0",
    "@types/jest": "^29.2.5",
    "@types/node": "^18.11.18",
    "jest": "^29.3.1",
    "prettier": "2.8.3",
    "ts-jest": "^29.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "commander": "^9.4.1"
  }
}
